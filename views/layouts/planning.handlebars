<div class="text-center">
  <h2>{{Exercisename}}</h2>
  <p>{{{exerciseData}}}</p>
  <p>{{description}}</p>
  <h5>Created by {{user.name}} on {{format_date date_created}}</h5>
  <p>
    <span for="img" id="gif"></span>
    <span class="instructions">{{instuctions}}</span>
    
  </p>
  <div>
    <button class="btn btn-primary">add to que</button>
  </div>
</div>
